---
version: 2
mergeable:
  - when: pull_request.*, pull_request_review.*
    name: 'checkfirst'
    validate:
      - do: title
        must_exclude:
          regex: '[A-Z][A-Z0-9]+-\d+'
          regex_flag: none
          message: 'Branch name contains Jira ticket (i.e EES-2021)'